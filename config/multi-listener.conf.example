# Glider Multi-Listener Mode Configuration Example
# Each listener has its own dedicated forwarders
# All configurations are in ONE file!
# Format: listen1/forward1, listen2/forward2, listen3/forward3, ...

# Basic configuration
verbose=True

# Enable multi-listener mode
useMultiListenerMode=true

# API server configuration
serverPort=6777

# SXX Proxy configuration (optional)
sxxhost=https://api.sx.org
sxxkey=your_sxx_key_here

# ============================================
# Listener Group 1: US Proxy (Port 10800)
# ============================================
listen1=:10800
forward1=http://username:password@us-proxy-host:9999
strategy1=rr
check1=http://www.msftconnecttest.com/connecttest.txt#expect=200
checkinterval1=3000

# ============================================
# Listener Group 2: UK Proxy (Port 10801)
# ============================================
listen2=:10801
forward2=socks5://username:password@uk-proxy-host:9999
strategy2=rr
check2=http://www.msftconnecttest.com/connecttest.txt#expect=200
checkinterval2=3000

# ============================================
# Listener Group 3: Direct Connection (Optional)
# ============================================
# listen3=:10802
# forward3=direct://
# strategy3=rr

# ============================================
# Notes:
# ============================================
# - Each listener group uses numbered parameters: listen1/forward1, listen2/forward2, etc.
# - Maximum 100 listener groups supported (listen1-listen100)
# - Required parameters:
#   - listenN: listen address
#   - forwardN: forward proxy (at least one)
# - Optional parameters:
#   - strategyN: forwarding strategy (rr/ha/lha/dh)
#   - checkN: health check URL
#   - checkintervalN: health check interval in seconds
# - Each listener can only have ONE forward proxy in current implementation
#   (multiple forwards not yet supported in numbered parameter mode)

# ============================================
# Usage:
# ============================================
# Run: glider -config glider.conf

