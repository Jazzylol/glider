# Glider Multi-Listener Mode Configuration Example - Main Config File
# Each listener has its own dedicated forwarders
# 
# IMPORTANT: Listener group configurations should be placed in a SEPARATE file
# to avoid conflag validation errors.

# Basic configuration
verbose=True

# Enable multi-listener mode
useMultiListenerMode=true

# Listener groups configuration file (REQUIRED when useMultiListenerMode=true)
# This file contains all listener.N.xxx parameters
listenerConfig=listeners.conf

# API server configuration
serverPort=6777

# SXX Proxy configuration (optional)
sxxhost=https://api.sx.org
sxxkey=your_sxx_key_here

# ============================================
# Usage:
# ============================================
# 1. Create glider.conf with the above content
# 2. Create listeners.conf with listener group configurations
#    (see listeners.conf.example for format)
# 3. Run: glider -config glider.conf
#
# See listeners.conf.example for listener group configuration format

